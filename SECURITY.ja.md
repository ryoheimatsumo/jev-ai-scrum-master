# セキュリティモデル

[English](SECURITY.md)

このα版は、**信頼できるローカルリポジトリ**の証拠と開発手順を管理します。
敵対的コードの実行サンドボックス、改ざん不能な監査システム、正しさの認証ではありません。

## 実行と承認の境界

登録したコマンドは、環境変数を制限し、別HOME、時間・出力上限、プロセスグループの終了処理を使って、
作業用コピーで実行します。ただし、そのコードはOSユーザーが扱えるネットワークやファイルへアクセスできます。
環境変数を引き継がなくても、ディスク上の認証情報を読める場合があります。本番データや本番認証情報を使わないでください。

MCPから人間の確認を登録する入口は設けていません。計画承認に限り、表示済みの現行計画への
明示的なチャット指示を受けたエージェントが、ハッシュに結び付いた承認を記録できます。
記録されるのはエージェントによる指示の申告であり、Coreがチャットの発言者を認証するものではありません。
他の承認には引き続きローカルターミナルが必要です。同じユーザーの悪意あるプロセスは、
ターミナルの偽装、Python・SQLiteの変更、状態の読み取りができます。
モデルが自由に承認・PASS・DONEを設定する一般的な迂回路はありません。
強い隔離には、別OSユーザー、コンテナ・VM、ファイル権限、ネットワーク制御が必要です。
本プロジェクトがそれらを自動構築するわけではありません。

Skillは作業手順であり、ホストの全行動を強制するものではありません。完了判定が制御するのは
本ツールのDONE状態で、別経路のGitマージ、デプロイ、コマンド実行ではありません。

## 証拠とファイル

Runnerが取得したレポートでも、テストが正直である証明にはなりません。悪意あるテストは結果を偽造できます。
assertionと基準・ケースの対応を確認してください。ハッシュは変更を検出しますが、作成者を証明しません。
パス逸脱とシンボリックリンクを拒否し、XMLはdefusedxmlで扱い、実行前に古いレポートを除去します。
ただし、同一ユーザーによる敵対的なファイル競合を完全に隔離するものではありません。

スナップショットは未コミット・未追跡入力を含み、変更後は保守的に証拠を失効させます。
gateはファイルシステムのロックではないため、後続操作の直前にも確認します。
環境の指紋は、すべての依存パッケージ、動的ライブラリ、環境変数、外部サービスを含みません。
対象外の環境を変えた場合も、再検証が必要です。

`.env`や秘密鍵拡張子など既知のパスはコピー対象から除外しますが、完全な秘密情報検査ではありません。
除外した認証情報、外部ファイル、ローカルeditableパッケージに依存するテストは、完全には再現できません。

## 外部に送られるデータ

Coreは外部テレメトリーやJevを自動で有効にしません。Jevを有効にすると、選別した状態を
TypeSafeのサービスへ送信するため、先にそのデータの送信許可を確認してください。
応答、モデルID、使用量、ハッシュなどは記録され得ます。外部サービスとコーディングホストの規定は別です。

第三者のSkillsインストーラーとパッケージ取得も通信します。
[インストーラーの説明](https://skills.sh/docs/cli)にある`DISABLE_TELEMETRY=1`は、
そのインストーラーのテレメトリーだけを停止します。local-firstは完全オフライン推論を意味しません。

ログ共有前に内容を確認してください。ソース抜粋、個人情報、パス、ホスト名、マスクを逃れた秘密値を
含む可能性があります。生レポート、認証情報、顧客データ、内部URL、未加工の再現情報を公開しないでください。
プロジェクト横断の改善ルール共有は既定で有効にしていません。

## 導入・キャッシュ・復旧

同梱wheelは版とハッシュで確認しますが、発行者署名ではありません。依存関係も完全固定ではありません。
確認した導入元を使ってください。ローカルSkillインストーラーは未管理・編集済みファイルを保持し、
ホストの権限・指示・MCP・Hookを変更しません。上流インストーラーには独自の更新動作があります。経路を混在させないでください。

助言キャッシュはタスク・ワークスペース単位で、入力・契約・設定・質問・モデル・backendをキーにします。
生プロンプトを保存せず、保存済み助言をPASS・承認・新しい検証証拠として扱いません。
同時の初回要求ではAPIが重複する場合があります。キャッシュ削除で証拠や履歴は消えません。

親プロセスの強制終了で、RUNNINGのジョブや子プロセスが残る場合があります。自動のクラッシュ復旧は未実装です。
状態を保全し、残ったプロセスを停止し、実験には別ワークスペースを使ってください。
DONEを作るためにDBを編集しないでください。Skill更新中の中断も、手動確認が必要になる場合があります。

## 脆弱性の報告

攻撃の詳細や秘密情報を公開Issueへ書かないでください。リポジトリで有効になっている場合は、
GitHubの非公開脆弱性報告機能を使います。この機能が有効かどうかは、今回確認していません。
利用できなければ、秘密を含まない概要だけで、メンテナーに非公開の連絡先を確認してください。
専用のセキュリティ窓口や、返信時間の約束は現在公開していません。

実際の認証情報が漏れた場合は失効・再発行してください。ファイルを消すだけでは過去のコピーやGit履歴からは消えません。
これは対処方針であり、認証情報の漏洩を発見したという意味ではありません。
